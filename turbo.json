{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"build": {
			"outputs": ["dist/**", ".vercel/**"]
		},
		"check": {
			"dependsOn": ["^check"]
		},
		"dev": {
			"persistent": true,
			"cache": false
		}
	}
}
